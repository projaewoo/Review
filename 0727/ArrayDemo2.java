/*
작성자 : 
작성목적 : 
작성일시 : 
작성환경 : 
*/
import java.util.Scanner;

public class ArrayDemo2 {
	public static void main(String[] args) {  //배열 타입 : String, 배열 이름 : args
		System.out.println("배열 갯수 : " + args.length);
		/*float [] array = new float[4];
		Scanner scan = new Scanner(System.in);
		for(int i = 0 ; i < array.length ; i++) {		//반복횟수 확실히 알아서 for문
			System.out.print(">> : ");
			array[i] = scan.nextFloat();
		}
		float min = array[0], max = array[0];
		for(int i = 1 ; i < array.length ; i++) {			//min, max의 초기값이 0번째 배열값이므로
			if(min > array[i]) min = array[i];
			if(max < array[i]) max = array[i];
		}
		System.out.println("가장 큰 값 = " + max);
		System.out.println("가장 작은 값 = " + min); */
		
	}
}
